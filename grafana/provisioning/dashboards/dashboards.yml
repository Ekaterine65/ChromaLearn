apiVersion: 1

providers:
  - name: ChromaLearn
    orgId: 1
    folder: ChromaLearn
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
